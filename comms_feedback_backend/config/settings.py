# comms_feedback_backend/config/settings.py

# This file will eventually hold configuration settings like:
# ASR model paths, API keys, database connection strings,
# FFmpeg executable path (if not in PATH), etc.

# Example:
# class Settings:
#     FFMPEG_PATH = "/usr/local/bin/ffmpeg"
#     ASR_MODEL_NAME = "openai/whisper-tiny"
#
# settings = Settings()